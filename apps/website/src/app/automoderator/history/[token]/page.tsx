import { PublicHistory } from './_components/PublicHistory';

export default function AutomoderatorPublicHistoryPage() {
	return (
		<main className="mx-auto w-full max-w-2xl px-4 py-10">
			<PublicHistory />
		</main>
	);
}

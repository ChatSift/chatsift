// Fixture consumed by `registerComponentHandlers`' glob loader in components.test.ts -- a default export that
// isn't a zero-arg constructor with a `handle` method, so `isModuleWithDefault` should reject it.
export default { name: 'not-a-constructor' };

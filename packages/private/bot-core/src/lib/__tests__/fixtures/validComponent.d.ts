// Ambient declaration for the plain-JS fixture next to this file -- `tsc` builds with `allowJs: false`, so the
// static `import('./fixtures/validComponent.js')` in components.test.ts needs this to resolve a type.
export declare const calls: unknown[];

declare const FixtureValidComponent: unknown;
export default FixtureValidComponent;

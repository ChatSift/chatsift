{
	"$schema": "https://json.schemastore.org/package.json",
	"name": "@chatsift/core",
	"version": "0.1.0",
	"description": "Core utils and types for cross-service interaction",
	"type": "module",
	"exports": {
		".": "./dist/index.js"
	},
	"types": "./dist/index.d.ts",
	"scripts": {
		"build": "yarn run db:generate && tsc",
		"test": "vitest run",
		"test:watch": "vitest",
		"lint": "eslint src",
		"lint:fix": "eslint src --fix",
		"clean": "rimraf dist"
	},
	"devDependencies": {
		"@types/node": "^24.6.1",
		"typescript": "~5.9.3"
	}
}

{
	"$schema": "https://turbo.build/schema.json",
	"extends": ["//"],
	"tasks": {
		"build": {
			"inputs": ["src/**", "assets/**", "package.json", "tsconfig.json"]
		},
		"test": {
			"outputs": []
		}
	}
}
